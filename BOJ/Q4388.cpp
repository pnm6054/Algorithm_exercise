#include <iostream>
#include <string>
using namespace std;

int main(void)
{
	string a, b;
	int l;
	while (1)
	{
		cin >> a >> b;
		if (a == "0" && b == "0") break;
		l = a.length() < b.length() ? a.length() : b.length();
		for (int i = l - 1; i > )
	}
	return 0;
}