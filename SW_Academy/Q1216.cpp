#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main(void)
{
	int t;
	vector<string> v;
	string s;
	cin >> t;
	for (int i = 0; i < 10; i++)
	{
		cin >> s;
		for (int j = 1; j < 99; j++)
		{
			for (int k = 0; k < )
		}
		v.push_back(s);
	}
	
	return 0;
}